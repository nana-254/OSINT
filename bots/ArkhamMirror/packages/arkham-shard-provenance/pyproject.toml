[project]
name = "arkham-shard-provenance"
version = "0.1.0"
description = "Evidence chain tracking and data lineage shard for ArkhamFrame"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
    # Metadata forensics dependencies
    "Pillow>=10.0.0",            # Image EXIF extraction
    "pypdf>=4.0.0",              # PDF metadata extraction
    "python-magic>=0.4.27",      # File type detection (requires libmagic system library)
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
provenance = "arkham_shard_provenance:ProvenanceShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
