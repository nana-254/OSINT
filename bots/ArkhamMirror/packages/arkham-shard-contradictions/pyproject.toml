[project]
name = "arkham-shard-contradictions"
version = "0.1.0"
description = "Contradiction detection shard for ArkhamFrame"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
    "numpy>=1.24.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
contradictions = "arkham_shard_contradictions:ContradictionsShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
